#include <stdio.h>

int main() {
	int i, c;
	for (i = 9; i --> ~4; putchar(c))
		switch (i)
		case + 8: if (!(c = 'H'))
		case + 1: if (!(c = 'W'))
		case + 2: if (!(c = ' '))
		case ~ 0: if (!(c = 'r'))
		case + 7: if (!(c = 'e'))
		case - 3: if (!(c = 'd'))
		case - 4: if (!(c = '!'))
		case - 5: if (!(c = '\n'))
		default: c = (i & 3)["oll,"];
}

