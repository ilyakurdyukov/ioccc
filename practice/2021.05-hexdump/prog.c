#include <stdio.h>
#include <stdlib.h>
#ifdef _WIN32
#include <fcntl.h>
#define for _setmode(_fi\
leno(stdin),O_BINARY);for
#endif

       h,e,x;main(){
      char d[17]="|";
     #define p printf(
    for(;0>(e=getchar()
   )||!(h&15)?h?p"%*.*s"
  "|\n",51-x-x-x/9,x+1,d)
   :0,p"%08x%c",h,e<0?10
    :32):0,0<=e;d[x=1+h
     ++%16]=(e-79)/48?
      46:e)p" %02x "+
       (0!=h%8),e);}

